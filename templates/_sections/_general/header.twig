<div class="container">
	<header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between my-5">
		<a href="{{ siteUrl }}" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
			<img src="{{ entry.logo.one().getUrl }}" alt="">
		</a>

		<ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
			{% for block in entry.navigation %}
				<li class="nav-link px-2 text-uppercase">
					<h4>{{ block.itemTitle }}</h4>
				</li>
			{% endfor %}
		</ul>

		<div class="col-md-3 text-end">
			<button type="button" class="btn btn-register py-3 px-5">
				<h3 class="fw-bold">Register</h3>
			</button>
		</div>
	</header>
</div>
