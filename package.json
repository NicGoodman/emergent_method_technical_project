{
    "dependencies": {
        "bootstrap": "^5.0.0",
        "node-sass": "^6.0.0"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "scss": "node-sass --watch scss -o web/assets/css"
    }
}